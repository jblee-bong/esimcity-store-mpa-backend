<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.naverspabackend.mybatis.mapper.EsimPriceMapper">

    <select id="findById" parameterType="esimPriceDto" resultType="esimPriceDto">
        select * FROM TB_ESIM_PRICE
        where type = #{type}
    </select>


</mapper>